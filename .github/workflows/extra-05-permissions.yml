name: Extra 05 - Minimal permissions

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  security-check:
    runs-on: ubuntu-latest

  steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Comprobar permisos minimos
      run: |
        echo "Este workflow solo necesita leer contenido del repositorio" 
